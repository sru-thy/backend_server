{
  "name": "backend-server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "npm run build && node dist/app.js",
    "develop":"nodemon ./src/app.ts",
    "migration:generate" : "npm run build && typeorm migration:generate -d dist/db/postgres.db.js",
    "migration:run": "npm run build && npx typeorm migration:run -d dist/db/postgres.db.js",
    "migration:revert": "npm run build && npx typeorm migration:run -d dist/db/postgres.db.js"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "express": "^4.18.2",
    "pg": "^8.11.2",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.17",
    "typeorm-naming-strategies": "^4.1.0",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1"
  }
}
